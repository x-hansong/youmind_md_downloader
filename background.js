// background.js (v1.6 with popup support)

// Since we're using popup, we don't need the onClicked listener anymore
// The popup will handle the communication with content script directly

console.log("[GEMINI-DOWNLOADER-BG] Background script loaded. Using popup interface.");
